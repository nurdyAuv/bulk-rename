pub enum NamingMode {
    Keep,
    Remove,
    Reverse
}

pub enum NumberingMode {
    None,
    Prefix,
    Suffix,
    PrefixAndSuffix,
    Insert
}

pub enum CaseMode {
    Same,
    Upper,
    Lower
}